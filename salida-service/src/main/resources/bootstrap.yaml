spring:
  application:
    name: salida-service
  cloud:
    config:
      uri: http://localhost:8081
  datasource:
    url: jdbc:mysql://localhost:3306/salida_db?useSSL=false&serverTimezone=UTC
    username: root
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
